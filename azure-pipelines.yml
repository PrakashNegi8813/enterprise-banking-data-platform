trigger:

- main

pool:

  vmImage: ubuntu-latest

steps:

- checkout: self

- script: |
    pip install -r requirements.txt
  displayName: Install Dependencies

- script: |
    pytest
  displayName: Run Unit Tests

- script: |
    echo "Deploy Databricks Bundle"
  displayName: Deploy